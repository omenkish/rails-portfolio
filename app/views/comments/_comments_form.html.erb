<%= form_for @comment, url: '#' do |f| %>
  <div class="form-group">
    <%= f.label :content %>
    <%= f.text_area :content, class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= f.submit 'Post Comment', class: 'btn btn-dark' %>
  </div>
<% end %>